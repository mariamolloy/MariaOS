var TSOS;
(function (TSOS) {
    var scheduler = /** @class */ (function () {
        function scheduler() {
            //to do add cpu, quantum etc
        }
        return scheduler;
    }());
    TSOS.scheduler = scheduler;
})(TSOS || (TSOS = {}));
